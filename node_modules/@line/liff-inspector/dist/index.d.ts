import { LiffPlugin, LiffPluginContext } from '@line/liff';
export declare type LIFFInspectorPluginOption = {
    origin?: string;
};
export declare const getBackendUrl: (liffId: string, origin: string) => string;
export declare const getOriginFromUrl: (search: string) => string | null;
export declare class LIFFInspectorPlugin implements LiffPlugin<void> {
    private option?;
    name: string;
    private client;
    constructor(option?: LIFFInspectorPluginOption | undefined);
    private connect;
    install({ liff }: LiffPluginContext): void;
}
export default LIFFInspectorPlugin;
