import Protocol from 'devtools-protocol';
import { RemoteObject } from './RemoteObject';
export declare class PropertyDescriptor implements Protocol.Runtime.PropertyDescriptor {
    value?: RemoteObject;
    name: string;
    enumerable: boolean;
    configurable: boolean;
    writable?: boolean;
    constructor(value: unknown, objectId: string | undefined, { name, enumerable, configurable, writable, }: Pick<Protocol.Runtime.PropertyDescriptor, 'name' | 'enumerable' | 'configurable' | 'writable'>);
}
