import Protocol from 'devtools-protocol';
export declare class Response implements Protocol.Network.Response {
    url: string;
    status: number;
    statusText: string;
    headers: Record<string, string>;
    mimeType: string;
    connectionReused: boolean;
    connectionId: number;
    encodedDataLength: number;
    securityState: "secure";
    constructor({ url, status, statusText, headers, mimeType, encodedDataLength, }: {
        url: string;
        status: number;
        statusText: string;
        headers: Record<string, string>;
        mimeType: string;
        encodedDataLength: number;
    });
}
