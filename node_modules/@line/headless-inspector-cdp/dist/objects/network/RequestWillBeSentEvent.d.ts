import Protocol from 'devtools-protocol';
import { Initiator } from './Initiator';
import { Request } from './Request';
export declare class RequestWillBeSentEvent implements Protocol.Network.RequestWillBeSentEvent {
    requestId: string;
    loaderId: string;
    documentURL: string;
    request: Request;
    timestamp: number;
    wallTime: number;
    initiator: Initiator;
    redirectHasExtraInfo: boolean;
    type?: Protocol.Network.ResourceType;
    frameId?: string;
    hasUserGesture?: boolean;
    constructor({ requestId, documentURL, request, timestamp, type, }: {
        requestId: string;
        documentURL: string;
        request: {
            url: string;
            method: string;
            headers: Record<string, string>;
        };
        timestamp: number;
        type?: Protocol.Network.ResourceType;
    });
}
