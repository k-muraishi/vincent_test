import Protocol from 'devtools-protocol';
export declare class Initiator implements Protocol.Network.Initiator {
    type: 'parser' | 'script' | 'preload' | 'SignedExchange' | 'preflight' | 'other';
    url?: string;
    lineNumber?: number;
    columnNumber?: number;
    requestId?: string;
    constructor({ type, url, lineNumber, columnNumber, requestId, }: Protocol.Network.Initiator);
}
