export declare class Highlight {
    node: HTMLDivElement;
    constructor(append: (el: HTMLDivElement) => void);
    hide(): void;
    show(target: Element, { contentColor }: {
        contentColor: string;
    }): void;
}
