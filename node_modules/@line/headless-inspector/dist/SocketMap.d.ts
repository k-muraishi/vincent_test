declare class SocketPool<T> {
    devtool: Set<T>;
    client: Set<T>;
    add(key: 'devtool' | 'client', data: T): void;
    delete(key: 'devtool' | 'client', data: T): void;
}
export declare class SocketMap<T> {
    private map;
    set(key: string, socket: T, type: 'devtool' | 'client'): void;
    get(key: string): SocketPool<T> | undefined;
    remove(key: string, socket: T, type: 'devtool' | 'client'): void;
    delete(key: string): void;
}
export {};
