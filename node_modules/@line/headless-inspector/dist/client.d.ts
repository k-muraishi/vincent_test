import { Inspector } from '@line/headless-inspector-core/src';
export declare class HeadlessInspectorClient {
    private inspector;
    private ws?;
    private connected;
    constructor(inspector: Inspector<Record<any, any>, Record<any, any>>);
    private onWsClose;
    private onWsError;
    private onWsOpen;
    private onAllDebugEvents;
    private onMessageFromDevTools;
    private send;
    enable(wsUrl: string): void;
    disable(): void;
}
